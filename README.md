# Histogram-Analysis
Deep dive into database internals

To run id_title_plotter.py run "python3 id_title_plotter.py" if on MacOS, or it's equivalent on Linux/Windows, likely "python id_title_plotter.py".

